apiVersion: v1
kind: Service
metadata:
  name: {{ include "backend-volume.fullname" . }}
  labels:
    {{- include "backend-volume.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backend-volume.selectorLabels" . | nindent 4 }}
