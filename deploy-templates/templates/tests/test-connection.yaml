apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-volume.fullname" . }}-test-connection"
  labels:
    {{- include "backend-volume.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-volume.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
